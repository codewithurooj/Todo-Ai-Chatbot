# Backend Environment Variables

# Database Connection
# INSTRUCTIONS:
# 1. Go to https://neon.tech and create account
# 2. Create new project called "todo-ai-chatbot"
# 3. Copy the connection string from the dashboard
# 4. Paste it below
DATABASE_URL=postgresql://YOUR_USER:YOUR_PASSWORD@YOUR_HOST.neon.tech/YOUR_DB?sslmode=require

# Authentication (must match frontend)
# INSTRUCTIONS: Generate with: openssl rand -base64 64
BETTER_AUTH_SECRET=YOUR_SECRET_KEY_HERE

# OpenAI API
# INSTRUCTIONS: Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_API_KEY_HERE

# MCP Server Configuration (if running separately)
MCP_SERVER_URL=http://localhost:8000

# Application Configuration
ENVIRONMENT=development
LOG_LEVEL=INFO

# CORS Configuration
# Comma-separated list of allowed origins for Cross-Origin Resource Sharing
# Development: Include all local development ports
# Production: Replace with your deployed frontend URLs (e.g., https://your-app.vercel.app)
# IMPORTANT: Wildcards (*.vercel.app) are NOT supported - specify each origin explicitly
CORS_ORIGINS=http://localhost:3000

# Rate Limiting
RATE_LIMIT_PER_HOUR=100
RATE_LIMIT_PER_MINUTE=20
