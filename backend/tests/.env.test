# Test Environment Configuration
# This file is used by pytest for test database and services

# Test Database (SQLite for faster tests, or separate PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/todo_chatbot_test

# Authentication (test secret)
BETTER_AUTH_SECRET=test-secret-key-minimum-32-characters-long-for-security
BETTER_AUTH_URL=http://localhost:3000

# OpenAI (mocked in tests, but required by settings validation)
OPENAI_API_KEY=sk-test-mock-key-for-testing-purposes-only
OPENAI_MODEL=gpt-4o-mini

# MCP Server
MCP_SERVER_NAME=todo-chatbot-mcp-test
MCP_SERVER_VERSION=1.0.0

# CORS (test frontend)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# API Settings
API_V1_PREFIX=/api
PROJECT_NAME=Todo AI Chatbot API (Test)
PROJECT_VERSION=3.0.0

# Application
DEBUG=True
LOG_LEVEL=DEBUG
